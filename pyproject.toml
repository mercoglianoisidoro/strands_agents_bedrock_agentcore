[project]
name = "strands-agents-workspace"
version = "0.1.0"
description = "Strands Agents Monorepo"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["local-agents", "shared", "remote-agentcore", "agentcore_client"]

[tool.uv]
dev-dependencies = []
